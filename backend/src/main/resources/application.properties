# Firestore is disabled by default so local tests/runs do not require Firebase credentials.
# Set FIRESTORE_ENABLED=true (or firestore.enabled=true) to persist NOTAMs to Firestore.
firestore.enabled=${FIRESTORE_ENABLED:false}
